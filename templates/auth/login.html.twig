{# @var last_username #}
{# @var error #}
{% extends 'base.html.twig' %}
{% block title %} Micro Posts Page {% endblock %}

{% block body %}
    {% if error %}
        <div class="col-12 alert alert-danger">
            {{ error.messageKey | trans(error.messageData, 'security') }}
        </div>
    {% endif %}
        <div class="example-wrapper">
            <h1>Login on Our Site ✅</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
{% endblock %}